<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="BlogBack.mapper.TagMapper">
    <insert id="insertBatch">
        <if test="tagIds != null and tagIds.size() > 0">
            INSERT INTO article_tag (article_id, tag_id) VALUES
            <foreach collection="tagIds" item="tagId" separator=",">
                (#{articleId}, #{tagId})
            </foreach>
        </if>
    </insert>
    <select id="pageQuery" resultType="BlogBack.pojo.vo.TagPageQueryVO">
        select tag.*,count(id) article_count
        from tag left join article_tag at
        on tag.id = at.tag_id
        group by tag.name,tag.create_time
        order by tag.create_time desc
    </select>
</mapper>