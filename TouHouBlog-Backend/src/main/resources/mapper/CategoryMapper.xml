<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="BlogBack.mapper.CategoryMapper">
    <select id="pageQuery" resultType="BlogBack.pojo.entity.Category">
        select c.id,c.name,count(a.id) article_count,c.create_time from category c left join article a on c.id=a.category_id group by c.id,c.name,c.create_time order by c.create_time desc
    </select>
</mapper>